# Sample configuration
samples:
  sample_list: "snakeotate/resources/snv_annotation/sample_list.csv"
  
base_dir: "snakeotate/samples"

thresholds:  # Fixed: was "threasholds"
  af_threshold: 0.006
  cadd_threshold: 21

# Other configuration options can go here
# output:
#   base_dir: "samples"
# tools:
#   some_tool: "/path/to/tool"